<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android2.rent_a_space">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Rent_a_space">
        <activity android:name=".ViewForCustomer"></activity>
        <activity android:name=".ViewPropertyDetails" />
        <activity android:name=".PostYourPropertyScreening" />
        <activity android:name=".AdminSpaceRentActivity" />
        <activity
            android:name=".UserSpaceRentPage"
            android:label="RENT SPACE"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Registration" />
        <activity android:name=".login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>